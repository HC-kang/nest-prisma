export const strings = {
  // Common
  common: {
    // Errors
    errors: {
      // Unauthorized
      unauthorized: 'You are not authorized to do this',
      // Not found
      notFound: 'Record not found',
      // Already exists
      alreadyExists: 'A record with the same key already exists',
      // Relation does not exist
      relationDoesNotExist: 'The relation does not exist',
      // Required value is null
      requiredValueIsNull: 'The required value is null',
      // Unexpected error
      unexpectedError: 'An unexpected error occurred',
    },
  },
};
